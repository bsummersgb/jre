<section class="main">

  <h1 class="index-header">Guests Index Page</h1>

  <% @guests.each do |guest| %>
    <div>
      <%= link_to "#{guest.first_name} #{guest.last_name}", guest_url(guest) %>
    </div>
  <% end %>
</section>
